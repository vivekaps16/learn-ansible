- name: get vault password
  hosts: localhost
  connection: local
  tasks:
    - name: Print password
      ansible.builtin.debug:
        msg:  "{{ lookup('hashi_vault', 'test/data/credentials:password token={{ vault_token}} url=http://172.31.85.68:8200 validate_certs=False')}}"   